@model IEnumerable<DBModel_DAL.ProfileModel>

@{
    ViewBag.Title = "CV";
}

<h2>CV</h2>
<h3> Création / Edition (manuelle)</h3>


<p>
    @Html.ActionLink("CREATE NEW CV", "Create")
</p>
@*@using (Html.BeginForm("Index", "Profiles", FormMethod.Get))
    {
    <p>
        Genre: @Html.DropDownList("profilGenre", "All")
        Nom: @Html.TextBox("SearchString") <br />
        <input type="submit" value="Filtrer" />
    </p>
    }*@
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.FisrtName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.LastName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Gender)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Adress)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.BirthOfDate)
        </th>

        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FisrtName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LastName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Gender)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Adress)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.BirthOfDate)
            </td>

            <td>
                @ViewBag.Data
                @*@Html.Partial("_ViewHobbies", ViewBag.Result)*@



                @*@Html.ActionLink("Hobbies", "GetHobbies", new { id = item.ProfileID }) |*@
                @Html.ActionLink("Edit", "Edit", new { id = item.ProfileID }) |
                @Html.ActionLink("Details", "Details", new { id = item.ProfileID }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.ProfileID })
            </td>
        </tr>
    }

</table>
